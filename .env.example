# PostgreSQL connection string
DATABASE_URL=postgresql://taxledger:taxledger@localhost:5432/taxledger?schema=public

# HTTP server
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# Signed session tokens (use a long random string in production)
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=8h

# Simulated payment gateway credentials
PAYMENT_GATEWAY_KEY=pk_test_replace_me

# Local document storage
UPLOAD_DIR=uploads
